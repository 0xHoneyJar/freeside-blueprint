{
  "name": "freeside-blueprint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Non-runtime Freeside intent, observation, decision, and visualization repository.",
  "scripts": {
    "check": "tsx src/check.ts",
    "render": "tsx src/render.ts",
    "test": "tsx src/test.ts",
    "all": "npm run check && npm run render && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "yaml": "^2.8.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
