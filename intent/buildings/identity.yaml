schema_version: 1
id: identity
name: Identity
purpose: Create/link user accounts after report delivery.
owns:
  - user_identity
  - credentials
  - session
accepts:
  - ClaimAudit
produces:
  - identity_resolved
never:
  - block anonymous audit generation
  - own scores or audit artifacts
runtime_policy: optional-post-value-existing-service
