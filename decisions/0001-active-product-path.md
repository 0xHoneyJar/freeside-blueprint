# 0001 — Active product path

Status: proposed by operator intent

The first active path is:

```text
Dashboard -> Ordering -> Sonar -> Score -> Ordering audit engine -> Dashboard
```

The audit engine is co-located with Ordering. Identity, Discord role snapshots, Worlds, MCP federation, and NATS are not required for the first report.
