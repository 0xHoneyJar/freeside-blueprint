# 0003 — Contract resolution is query-first

Status: proposed

Because the MVP input is chain + contract address, canonicalization and capability checks are synchronous queries during the order's resolving phase.

A durable CollectionResolution actor is deferred until Freeside accepts ambiguous names, URLs, several deployments, or a user choice that must survive sessions.
