# Agents

Read `human/SYSTEM.mmd` and `human/products/access-audits.md`.

Trace one requirement ID.

Append proof to `agent/work.jsonl`.

Stop at the first unexpected result.

Create or update one issue in the owning repository.

Never edit `human/`.

Never implement fixes from this repository.

Never rewrite existing JSONL lines.
